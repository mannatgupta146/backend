services:
  backend:
    build: ./backend
    ports:
      - "8080:3000"
    volumes:
      - ./backend:/app
      - backend_node_modules:/app/node_modules
    command: npx nodemon -L /app/server.js

volumes:
  backend_node_modules: